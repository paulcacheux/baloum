name: "Run conformance tests"

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Checkout conformance repository
        uses: actions/checkout@v3
        with:
          repository: Alan-Jowett/bpf_conformance
          path: conformance

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libboost-all-dev

      - name: Setup Go
        uses: actions/setup-go@v3
        with:
          go-version: '1.19'

      - name: Go build test driver
        run: |
          go build -o test_driver ./conformance_tests

      - name: Help
        run: |
          ./test_driver -help

      - name: Build conformance checker
        working-directory: conformance
        run: |
          cmake -S . -B build
          cmake --build build

      - name: Run checker
        run: |
          ./conformance/build/src/bpf_conformance --test_file_directory ./conformance/tests --plugin_path ./test_driver
